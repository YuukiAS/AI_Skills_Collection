---
schema: AI_BRIDGE_REVIEWED_REQUEST_V1
task_key: 030_stage3_visual_recovery
---

# 030 Stage 3 Visual-Maturity Recovery — Request

## Why this task exists

Stage 3 的核心工程链已经由 027 建立并由真实 CI / compile / render / Terra evidence 验证：normal Stage 2 selector -> gold recipe -> CUHK content-space resolver -> native LaTeX/TikZ/figure/image objects -> canonical exact CUHK Beamer compile/render。027 第二轮 review 已确认 statistical-model 页与 negative-result 页达到 mature bar，但 quantitative result、experiment design、medical comparison、next experiment 四类页面仍没有达到冻结的投影可读性与成熟科研构图标准。

027 已合法达到两轮 review limit，必须保留其 `REVIEW_LIMIT / REVISE` 历史，不能伪造 REVIEW_3。029 已独立 PASS，AI_Skills_Collection 现在具备 task-local Visual Review consumer contract，因此本 recovery 应直接使用该正常路径，不再创建显式 workflow_dispatch 补救任务。

## Product outcome

本任务只补齐 Stage 3 最后四类未成熟 executable layout primitive，并用 fresh real-pixel item/page-level Terra evidence 验证六个主要 Stage 3 内容页在同一 recovery identity 下达到 mature doctoral research-group-meeting / strong conference-talk bar。

如果 030 独立 PASS，Planner 才可把 Stage 3 整体关闭并进入 Stage 4 — One-Call Production Entry + Bounded Quality Loop。

## Scope

允许：

- 保留并复用 027 已通过的 exact CUHK、Stage 2 selector/recipe、geometry transfer、capacity/SPLIT_REQUIRED、native model page、negative-result page、audience-meta leak gate；
- 只针对四个已定位 blocker 增加可复用、job-specific executable primitives：presentation-native quantitative result、typed experiment design、same-case medical error crop/zoom、evidence-to-decision next experiment；
- 重新生成 027 Stage 3 regression 的真实 TeX/PDF/PNG 与机械证据；
- 创建 030 task-local visual manifest，并使用 `visual_review_required` contract 自动等待 GitHub Actions Terra evidence；
- 对六个主要内容页做 fresh item/page-level visual review。

禁止：

- 修改 027/028/029 的历史 REVIEW/CURRENT/FINAL_REPORT；
- 降低 mature bar 或把机械/CI/top-level Terra PASS 当视觉质量 PASS；
- 扩 Stage 2 gold corpus 或做新的 source scouting；
- 恢复 023 PPTX design-profile 路线；
- 开始 Stage 4 production entry 或 Stage 5 holdout；
- 为四个 fixture 页面写 test-specific 坐标硬编码，绕过现有 gold recipe / CUHK resolver；
- 回退到 generic rounded cards、box-arrow、不可读 raster、文字假装 image zoom。
