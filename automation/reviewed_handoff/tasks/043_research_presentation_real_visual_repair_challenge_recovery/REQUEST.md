# Reviewed Handoff Request — 043_research_presentation_real_visual_repair_challenge_recovery

## Objective

Close the single remaining generic evidence gap after 042 without reopening the failed 041 holdouts or lowering presentation quality: prove on independent non-holdout/public-safe material that a **real task-local Terra substantive-page finding** can flow through the 042 shared canonical scientific-object semantics into the already-shipped bounded quality-loop mapper, execute at most one existing repair cycle, change real render inputs and rendered pixels, and then pass fresh final item/page-level plus deck-level Terra review.

042's semantic compatibility implementation and its final all-PASS stress render are preserved as valid non-regression evidence. 042 itself remains terminal because its revised initial Terra produced no repairable substantive finding, so the frozen live-repair gate was never exercised. 043 must use a new finite validation mechanism rather than repeatedly changing the 042 fixture until a finding appears.

## User-provided inputs

- Program source of truth: `automation/reviewed_handoff/tasks/RESEARCH_PRESENTATION_CORPUS_PROGRAM_GOAL.md`.
- Failed frozen-batch history: `041_research_presentation_frozen_four_paper_generalization_batch`; all four papers are consumed holdouts and must not be tuning fixtures.
- Generic repair execution recovery: `039_research_presentation_quality_loop_execution_recovery`.
- Semantic compatibility recovery and terminal evidence: `042_research_presentation_semantic_compatibility_recovery`.
- Existing task-local Visual Review contract and GitHub Actions secret path already used by 042.

## User constraints

- Preserve the highest frozen mature doctoral-group-meeting / strong paper-talk visual bar; do not relax Terra requirements to obtain a repair finding or a PASS.
- Do not use 041 paper titles, DOI, text, figures, rendered pages, page-specific labels, or scientific content as fixture/tuning material.
- Do not add or modify mature gold records, repair vocabulary, canonical semantic roles, selector hard constraints, CUHK identity, source-fidelity rules, or the one-cycle repair limit merely to make this validation succeed.
- Initial challenge material must be fully pre-frozen before the first Terra result is seen. No adaptive challenge editing after visual feedback.
- Real Terra evidence is mandatory. Hand-authored/synthetic review JSON may be used only as unit-test fixtures and can never satisfy the live visual-repair acceptance gate.
- Use the existing Reviewed Handoff task-local Visual Review contract; if cloud evidence requires staged publication, use the already-supported watcher/GitHub Actions bridge rather than inventing a new state machine.
- 043 PASS would complete only the generic recovery after failed batch 041. Before any new fresh four-paper Stage 5 batch is frozen or consumed, the Program must stop at the user human gate required by the Program Goal.
