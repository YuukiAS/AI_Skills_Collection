---
schema: AI_BRIDGE_REVIEWED_REQUEST_V1
task_key: 035_research_presentation_generic_model_support_recovery
---

# Reviewed Handoff Request — 035_research_presentation_generic_model_support_recovery

## Objective

关闭 034 第二轮独立审核发现的唯一剩余 blocker：共享 `STATISTICAL_MODEL` equation-support renderer 必须只使用当前 page-job/source 支持的信息，不能因为 Stage 4 engineering fixture 恰好是 clustered interval calibration，就把 `ICC`、center variation、interval comparison 或制作型 `source-grounded` fallback 硬编码到所有未来统计模型页。

完成后，当前 engineering deck 仍应保持成熟模型页和已通过的整套 deck 节奏；同时至少一个与 clustered calibration 完全无关的 model regression 必须证明同一共享路径不会泄露当前 fixture 的科学语义。

## User-provided inputs

- `RESEARCH_PRESENTATION_CORPUS_PROGRAM_GOAL.md` 的 source-fidelity、no-hardcode、mature-quality 与 Quality-Preserving Continuation Policy。
- 034 的 frozen Plan、`REVIEW_1.md`、`REVIEW_2.md`、`FINAL_REPORT.md`、真实 CI 与 fresh Terra evidence。
- 当前 shared/plugin research-presentation production/layout code，以及 Stage 4 engineering regression bundle。

## User constraints

- 保留 034 的 review-limit 历史，不创建 `REVIEW_3`。
- 只处理 equation/model supporting copy 的通用 source-grounding；不得重做 dual identity、deck quality loop、storyline、gold library、CUHK identity、medical semantics 或增加 automatic repair 次数。
- 不降低成熟视觉门槛，不用当前 fixture 命中硬编码来证明通用性。
- 不运行 Stage 5 holdout；unrelated model regression 只能是 public-safe synthetic/test evidence。
- shared skill source 与 Codex marketplace mirror 必须保持 parity。
- 若当前 engineering deck 像素变化，使用现有 task-local Visual Review contract 获取 fresh item/contact-sheet evidence；不得复用不匹配的旧像素审核。
- 这不是用户产品/科学选择，不需要用户介入；若在本任务有限边界内无法取得新增证据，再按 Program Goal 判断是否需要新的 bounded recovery。
