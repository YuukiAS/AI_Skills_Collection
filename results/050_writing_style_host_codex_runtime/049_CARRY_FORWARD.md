# 049 -> 050 carry-forward

## Status

049 is `SUPERSEDED`. It is not Product PASS and receives no further paid smoke work.

## Keep / selectively reuse

- source-span / argument-unit concepts;
- Meaning Card / Fidelity Ledger semantics;
- `inline-critical` vs `relocatable-trace` literal roles;
- Reader Core / Technical Trace separation;
- deterministic literal verification and privacy checks;
- isolated/shadow installed-entrypoint replay rules;
- private authorization boundaries;
- fixed SMOKE-A/B/C source identities and reader questions;
- dataflow evidence concepts where they prove real host-Codex work rather than count model calls.

## Retire from production architecture

- Terra/OpenAI calls for Document Map, segmentation, Meaning Cards, writer, semantic audit, repair or assembly;
- `api.openai.com` as a normal `scientific-rewrite` generation dependency;
- push-triggered paid private transform;
- automatic paid review/repair loops;
- source-copy completion of missing semantic meaning;
- model-call count as evidence of reader quality.

## Replacement ownership

```text
host Codex + installed writing-style -> semantic/writing work
local deterministic tooling          -> exact/privacy/evidence checks
Terra                                 -> optional bounded final candidate-only QA
user                                  -> required style/final acceptance
```

## Cost contract

Task 050 permits at most two Terra QA calls total, USD 0.50 hard cumulative campaign budget, USD 0.30 worst-case preflight per call, zero automatic paid retries, and no paid invocation on ordinary Git push.
