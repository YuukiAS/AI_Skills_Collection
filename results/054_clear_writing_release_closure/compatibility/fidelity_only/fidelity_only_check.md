原文中的指标、公式、路径和实验适用范围均需要保护。最主要的保真风险是把仅用于 FedAvg 消融实验的正则项扩展为所有方法的共享设置。以下仅检查保真风险，不提供润色版本，也不修改正文。

| 核对项 | 必须保护的内容 | 保真风险 |
| --- | --- | --- |
| 数字与单位 | FedAvg 的 `Dice=0.81`、`HD95=7.4 mm` | 不得交换指标对应值、改变精度或遗漏 `mm`；不得擅自换算表示方式。 |
| 比较条件 | “同一划分设置下” | 不得删去该条件，也不能据此补充原文未给出的数据集、划分比例、随机种子或重复实验次数。 |
| 脚本路径 | `scripts/train_fedavg.py` | 必须逐字保留目录、文件名、下划线和扩展名，不能替换成推测的路径。 |
| 配置路径 | `configs/fedavg.yaml` | 必须逐字保留，不得将 `configs` 改为 `config`，或将 `.yaml` 改为 `.yml`。 |
| 损失公式 | $\mathcal{L}=\mathcal{L}_{seg}+0.1\mathcal{L}_{reg}$ | 必须保留等号、加号、系数 `0.1`、下标 `seg` 和 `reg`，不得擅自补充正则项的数学定义。 |
| 公式适用范围 | “这个正则项只用于 FedAvg 消融实验，不能写成所有方法共享设置” | 这是明确的范围限制，必须与公式保持关联；不能推断全部 FedAvg 实验都使用该正则项，也不能推广到所有方法。 |
| 结论强度 | 原文仅报告指定划分设置下的指标 | 不支持补写“显著提升”“优于其他方法”“稳定有效”或“具有泛化能力”等结论；原文没有提供基线、统计检验或跨设置证据。 |

仅凭所附文本，无法确认指标是否与实验记录一致、两个路径是否实际存在，或代码是否实现了该公式。原文也未明确这组指标是否来自使用该正则项的消融实验，不能自行建立这一对应关系。这些属于待验证的事实关系，不代表已经发现事实错误。
