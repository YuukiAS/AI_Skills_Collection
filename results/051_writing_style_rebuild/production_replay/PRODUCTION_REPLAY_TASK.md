请使用已安装的 `writing-style@yuukias-ai-skills` 插件，按普通用户会说的方式处理随附的
`public_rewrite_source.md`：把这份中文科研说明重新组织成自然、连贯、读者能直接理解的中文段落。

要求：

- 不要写摘要，不要加入新事实；
- 必须保留 `FedFisher`、`CARE`、`MMs`、`Dice=0.81`、`[12]`、公式、脚本路径和配置路径；
- 必须保留“一次通信设置”、公式只适用于 FedFisher 消融实验、CARE 没有使用这个正则项等条件边界；
- 不调用付费外部生成模型。

请在最终回答中说明实际触发的已安装插件能力、是否有付费外部生成调用，以及最终候选文本的
SHA-256。不要回显源文全文。
