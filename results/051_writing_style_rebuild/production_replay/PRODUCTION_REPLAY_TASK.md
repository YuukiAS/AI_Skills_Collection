请处理随附的中文科研说明，把它重新组织成自然、连贯、读者能直接理解的中文段落。

要求：

- 不要写摘要；
- 不要加入新事实；
- 保留 `FedFisher`、`CARE`、`MMs`、`Dice=0.81`、`[12]`、公式、脚本路径和配置路径；
- 保留“一次通信设置”、公式只适用于 FedFisher 消融实验、CARE 没有使用这个正则项等条件边界；
- 不调用付费外部生成模型。
