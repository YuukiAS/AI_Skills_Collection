# Project Thread Handoff Recovery v0.2 — Target Surface Acceptance

Status: `PENDING`

Final candidate branch:

`reviewed/science-communication--project-thread-handoff-recovery`

## Target Surface

- Account: user current Pro ChatGPT account.
- Product surface: ChatGPT regular Chat.
- Project: `AI Research Stack`.
- Distribution route: existing PRIVATE / USER-scope / skills-only Project Thread Handoff personal Plugin wrapper.
- Wrapper constraints: preserve existing Plugin identity, no create-new fallback, no MCP, no connector, no external API, no database, no app, no state.
- Icon: bundled `assets/app-facing.svg` must be byte-equivalent to the canonical source icon.

## G1 — Distribution / Explicit Invocation / Visual Identity

Status: `PENDING`

Evidence required:

- existing wrapper updated to the same final candidate commit;
- explicit regular Chat invocation works;
- wrapper remains skills-only and `MCP_ADDED = NO`;
- bundled icon is byte-equivalent to canonical icon.

## G2 — Current-Thread Handoff Regression

Status: `PENDING`

Evidence required:

- same final candidate runs Mode A in a target long thread;
- current context is consumed;
- latest user/frozen decision precedence is preserved;
- entity role, thread-only delta, canonical locator, exactly one initialization prompt, no second confirmation, and no repo write are observed.

## G3 — Same-Project Recovery / Generalization + Provenance

Status: `PENDING`

Evidence required:

- same final candidate runs Mode B in a new thread in the same `AI Research Stack` Project;
- target is the existing Bridge/SSH old conversation;
- user does not paste the full old answer set;
- target old conversation appears as an identifiable past-chat source or equivalent target-chat provenance;
- route-changing recovered claims are backed by that target source;
- generic Saved Memory, profile summary, or unsourced recall does not substitute for target-thread authority.

Allowed final claim after successful G3:

```text
target-conversation-backed semantic recovery verified; no observed cross-project substitution
```

Do not claim absolute isolation, raw transcript recovery, transcript-level fidelity, or visibility into every influencing source.

## Privacy Boundary

Do not commit real old thread text, complete private recovery output, private Plugin ID/URL, private chat URL, Project transcript dump, or unrelated Memory Sources. Repo evidence may record only minimal redacted criterion-level receipts.
