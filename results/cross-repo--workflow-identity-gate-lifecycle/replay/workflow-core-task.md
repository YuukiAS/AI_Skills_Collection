# Verified Workflow Candidate Replay Task

Use the installed Verified Workflow capability as you normally would for a complex repository workflow. Read the attached public input and produce a concise execution-readiness checklist.

The checklist must address:

- whether machine task identity and human labels are separated;
- whether release gate evidence maps regressions to existing gates before creating new gates;
- whether narrow versus broad/full gates are selected from source-scope evidence;
- whether final release claims require one final candidate.

Do not modify files. Do not fetch network resources. This is a public-safe candidate plugin replay.
