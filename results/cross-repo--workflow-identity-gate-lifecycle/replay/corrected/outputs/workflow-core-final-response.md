我会读取已安装的 Verified Workflow 技能和案例输入，按源码影响范围与证据覆盖范围逐项判断；不修改文件，也不访问网络。


---MESSAGE---

### W1：局部隔离变更

- **release_gate：`NARROW_OK`**
- 影响仅限独立 validator；直接运行 smoke 和相关测试通过，并有证据覆盖相邻行为不变，即可采用窄门禁。文件少本身不是依据，影响范围可解释地隔离才是。
- **同一最终候选证据：需要。**
- **固定付费／fresh 样本数：不要求。** 按冻结 Plan 和仓库规则选择门禁。

### W2：共享正常入口变更

- **release_gate：`BROAD_FULL_REQUIRED`**
- 改动跨越任务模板、初始化、通用 validator 和安装项目使用的生成文档；局部脚本测试不足。需要覆盖各正常入口、semantic task 创建、legacy validation 及生成内容的一致性。
- **同一最终候选证据：需要。**
- **固定付费／fresh 样本数：不要求。** broad/full 指覆盖范围，不等于固定调用次数。

### W3：多个必要门禁未满足

- **release_gate：`NOT_RELEASE_READY`**
- parity 和加载成功只是中间证据，不能替代实质输出审查及 scope semantics 门禁。后续源码已变更，旧 commit 的 PASS 不能与当前加载凭据拼接成发布证明。
- **同一最终候选证据：需要；当前不满足。**
- **固定付费／fresh 样本数：不要求。** 应补齐缺失门禁的有效证据，而非机械增加调用。

### W4：成熟度晋升请求

- **release_gate：`MATURITY_NOT_PROVEN`**
- 一次 replay 和两项确定性测试只能证明有限场景；缺少独立真实使用与用户验收历史，尚不足以支持 stable 晋升。
- **同一最终候选证据：发布门禁需要。** 历史真实使用可支持成熟度判断，但不能替代最终候选验证。
- **固定付费／fresh 样本数：不要求。** 冻结合同未规定次数，不应临时发明配额；应依据独立真实使用证据和验收合同判断。
