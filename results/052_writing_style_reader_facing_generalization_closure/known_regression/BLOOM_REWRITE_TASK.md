请把随附的布隆过滤器中文技术说明重新组织成自然、连贯、读者能直接理解的中文段落。

要求：

- 不要写摘要；
- 不要加入新事实；
- 保留 `Bloom Filter`、`Burton Howard Bloom`、`O(n)`、`O(log n)`、`O(1)`、`O(k)`、`m`、`k`、`ε`、引用标记和公式/符号；
- 保留“可能产生假阳性、不会产生假阴性”、简单布隆过滤器难以删除元素、辅助移除过滤器可能引入假阴性、假阳性率随元素数量增加等边界；
- 不要把正文写成“原文指出”“根据给定材料”“源文说明”这类改写过程说明；
- 不要调用付费外部生成模型。
