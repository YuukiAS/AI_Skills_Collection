请把随附的 Python 正则表达式模块说明改写成自然、连贯、读者能直接理解的中文技术说明。

要求：

- 不要写摘要；
- 不要加入新事实；
- 保留 `re`、`Perl`、`str`、`bytes`、`Unicode`、`SyntaxWarning`、`SyntaxError`、`regex`、`r"\n"`、`"\n"` 等关键对象和边界；
- 保留 Unicode 字符串与字节串不能混用、替换字串类型必须一致、反斜杠在正则表达式和 Python 字符串字面值中会冲突、原始字符串可避免额外转义、模块级函数是快捷方式但会损失微调参数等含义；
- 不要把正文写成“原文指出”“根据给定材料”“源文说明”这类改写过程说明；
- 不要调用付费外部生成模型；
- 写入 `outputs/python_re_docs.md`。
