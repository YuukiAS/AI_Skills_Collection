请把随附的快速傅里叶变换中文技术说明改写成自然、连贯、读者能直接理解的中文段落。

要求：

- 不要写摘要；
- 不要加入新事实；
- 保留 `FFT`、`DFT`、`O(n^2)`、`O(n log n)`、`O(N^2)`、`O(N log N)`、`X_k`、`N^2-N`、`N(N-1)`、`Cooley-Tukey`、`(N/2) log_2 N`、`N log_2 N` 等关键对象、公式和复杂度边界；
- 保留 FFT 与直接 DFT 结果相同但更快、许多 FFT 算法受舍入误差影响可能更精确、已知 FFT 算法复杂度为 `O(N log N)` 但无更低复杂度不可能的证明等含义；
- 不要把正文写成“原文指出”“根据给定材料”“源文说明”这类改写过程说明；
- 不要调用付费外部生成模型；
- 写入 `outputs/fft_wikipedia.md`。
