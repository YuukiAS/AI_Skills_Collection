# Verified Workflow Candidate Replay Task

Use the Verified Workflow plugin normally. Read the attached scenario file and
return a compact routing table for the Reviewed Handoff lifecycle cases.

For each scenario, name only the normal entry that should be used and whether
raw Git fallback is allowed. Also state what should happen when the required
Bridge capability or policy support is missing or old.

Do not propose a new workflow, local Git implementation, alternate clone,
alternate worktree, remote remap, or manual REQUEST/CURRENT construction.
