# Candidate Replay Task: AI Skills Maintainer Machine Update

Use AI Skills Maintainer.

Request: `update Bridge Kit`

Read the attached public-safe machine state note. Do not mutate files, Git refs, Marketplace state, Host state, or Bridge runtime. Return a concise dry-run style report that identifies:

1. which Maintainer route applies;
2. which internal Maintainer owner should handle Bridge distribution/version work;
3. why Bridge runtime/Host implementation must remain delegated to canonical `ai-bridge` commands;
4. the formal release/update-impact rule that prevents arbitrary `main` or diff inference from expanding scope;
5. the correct result state for a dry-run/no-mutation replay.

