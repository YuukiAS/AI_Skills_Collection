# Released AI Skills Maintainer G2 Smoke

Use the installed production plugin `ai-skills-core@yuukias-ai-skills`.

Normal user request:

```text
sync this machine
```

For this smoke, do read-only discovery and routing only. Do not mutate
Marketplace sources, plugin installation, Git refs, Bridge Kit refs, Bridge
runtime/source, Host state, project consumers, or unrelated repositories. Do
not call paid APIs.

Report whether the released AI Skills Maintainer normal entry is loaded, which
internal skill/route owns the request, the installed `ai-skills-core` version
and plugin path if visible, and the expected user-facing state for an already
updated current session.
