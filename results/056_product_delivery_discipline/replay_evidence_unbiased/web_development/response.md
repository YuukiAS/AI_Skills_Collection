The missing material state is an open product gap. A review can be delivered now; product closure requires evidence that the state and its real interaction work.

- **Authority to inspect:** Start with the canonical handoff at `readonly://bobbio/design-handoff/workspace-state-transition`. Identify its revision, exact surface/state, component variants, content, tokens, and interaction requirements. Compare these with the current implementation, relevant event/state logic, and available rendered evidence through read-only access. Use existing project components and preserve the canonical design intent; external inspiration is not a substitute for this authority.
- **Handle the gap:** Record the expected state, observed omission, affected user action, and acceptance criteria in a review handoff. The scenario does not identify which state is missing; do not invent an empty, loading, error, or other state. Once the authority is inspected, specify the smallest implementation change that restores the required behavior. If the authority leaves behavior ambiguous, flag that exact design decision for its owner rather than silently redesigning the flow.
- **Interaction and closure:** Trace the starting state → real user trigger → missing state → intended destination or recovery. Verification must exercise that path, including applicable feedback, focus/keyboard behavior, and reduced-motion behavior where animation exists. A static mockup, isolated component variant, or screenshot does not demonstrate a working transition. Keep the gap open until the rendered product matches the authority and the interaction succeeds; documented deferral must remain visible as unresolved scope.
- **Read-only limits and evidence:** This task can provide findings, proposed changes, and verification criteria under `outputs/`. It cannot implement or integrate a fix in the product repository. The supplied JSON establishes the omission as a scenario fact; no design contents, product source, or runtime evidence were supplied locally, and the locator has not been resolved here. Therefore this response does not claim independent reproduction, design parity, a repaired transition, passing tests, or product completion. The next review input is a locally accessible read-only export or mapping of the existing canonical locator, followed by implementation and interaction evidence from an authorized product task.

Method: used the installed Frontend Design (`web-development` 0.2) plugin through its declared skill entrypoints, routing to product UX planning, design handoff, and motion/interaction review. No network actions or product-repository modifications were performed.
