\documentclass[11pt]{article}
\usepackage{amsmath}
\usepackage{amssymb}
\begin{document}
\section*{Project Venue Fixture}
This repository-safe fixture represents a project-owned venue command. It uses
its own direct \LaTeX{} source and must not be rewritten into the canonical
Markdown profile.
\[
\operatorname{score}(\theta) = \sum_{i=1}^{n} x_i^\top \theta + \lambda.
\]
The output should be a real PDF with truthful project-command identity.
\end{document}
