请把输入文件中这段公式和表格为主的中文技术笔记整理成读者能直接理解的说明。

要求：

- 保留公式、变量含义、表格列名、表格数值、均值/标准差、比较关系和限制条件；
- 解释为什么选择 `epsilon=0.003`，但不要把它写成验证 RMSE 最低；
- 保持“下一步仍需更大外部测试集确认”的未完成状态；
- 使用 Markdown/LaTeX 保留公式，表格应能正常渲染；
- 中文要自然连贯，不要写成执行日志、审计说明或检查清单。

输出：

- 在指定 outputs 目录下写入 `g7_f2_formula_table.md`。
- 只输出最终 Markdown 成稿；不要附加自评、审计表、stage receipt、解释或对本任务的说明。
