# Project Thread Handoff V1 — Target Surface Acceptance

Status: `PENDING`

Final candidate branch: `reviewed/science-communication--project-thread-handoff`

Upload package:

`private/exports/project-thread-handoff-v1.zip`

Package SHA-256:

`0784e81f7141c99a0b79b5a6a48b2226ebe34f2e54bed1759662ffde167b28e1`

## Required Single Acceptance Session

After the final candidate is pushed and remote tip is verified, the user should:

1. Upload/install `private/exports/project-thread-handoff-v1.zip` from the current ChatGPT Skills page.
2. Return to the existing DII regular Chat thread.
3. Use the current interface's visible formal explicit Skill entry to invoke Project Thread Handoff once.

The same session must cover:

- G1 target-account install and explicit-entry observation.
- G2 DII regular Chat replay using existing thread context.

Do not require separate `$`, `@`, or `/` tests. Do not ask for a second upload unless this candidate fails and Planner/Critic authorizes a recovery path.

## Current Gate State

- G1 target account install / explicit entry: PENDING
- G2 DII semantic replay: PENDING
- G3 development representative regression: PASS

If regular Chat cannot invoke the installed Skill, only Work/Codex can invoke it, or the Skill cannot use current thread context, record `G2=FAIL` and return to Planner/Critic.
