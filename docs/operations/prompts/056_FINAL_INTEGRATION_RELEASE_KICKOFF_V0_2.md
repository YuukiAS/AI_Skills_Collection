# 056 Final Integration / Release Kickoff v0.2

Status: `READY_FOR_USER_AUTHORIZATION`

Critic PASS approves this execution package but does not execute it. The user sends this exact Kickoff only after that PASS.

**If this exact Kickoff is present as the current user message, that current message itself is the current-user-visible bounded authorization for every effect explicitly listed below.** Do not require an additional repo-local Critic PASS artifact. Do not stop merely because the repository's Plan, Goal, Review Package, or historical review files still contain planning/review metadata. Re-ask only if execution would introduce a genuinely new target, scope, destructive effect, provider/account/credential purpose, or cost outside this Kickoff.

## Kickoff

Continue historical task `056_product_delivery_discipline` for final integration/release closure only.

Read:
- `docs/design/056_FINAL_INTEGRATION_RELEASE_PLAN_V0_2_2026-09-22.md`
- `docs/goals/056_FINAL_INTEGRATION_RELEASE_CLOSURE_GOAL_V0_2.md`

Do not redesign 056 or rerun already-passed gates merely because SHAs change.

### 1. Frozen identities

AI:
- repo `YuukiAS/AI_Skills_Collection`
- reviewed branch `reviewed/056_product_delivery_discipline`
- production candidate `33c30bbe0dd528031a23d379905cd00d6b65bc1f`
- final evidence HEAD `b6f675869449df8daa86a6abcf527fbb72c66e64`
- target release: repo 5.0.7; workflow-core 0.3; web-development 0.2; ai-skills-core 0.4

Bridge:
- repo `YuukiAS/GPT_Codex_AI_Bridge_Kit`
- reviewed branch `reviewed/056_product_delivery_discipline`
- candidate `96a8ea1b58ebe6f9b7c5c46c43995666251911fe`
- target release 0.8.5

Exact temporary integration worktrees:
- `/overflow/htzhu/mingcheng_new/AI_Skills_Collection-056-final-integration`
- `/overflow/htzhu/mingcheng_new/GPT_Codex_AI_Bridge_Kit-056-final-integration`

Exact permanent Host:
- hostname `c0824.ll.unc.edu`
- user `aereinh`
- CODEX_HOME `/overflow/htzhu/mingcheng_new/.codex`

### 2. Current-user authorization

For this frozen Goal I authorize:

- create/use only the two exact temporary integration worktrees above;
- fetch the configured origins and merge the exact reviewed histories as specified;
- resolve only the pre-audited integration overlap;
- create release/closure commits;
- ordinary non-force push of the frozen Bridge release commit to Bridge `main`;
- ordinary non-force push of the frozen AI release commit and later final evidence-only closure commit to AI `main`;
- update the existing canonical normal Bridge root `/overflow/htzhu/mingcheng_new/GPT_Codex_AI_Bridge_Kit` by clean `git pull --ff-only origin main`;
- refresh its existing local editable Python installation in the same current environment when needed to make normal `ai-bridge` 0.8.5;
- permanently run released `ai-bridge host install` and `host validate` only against `/overflow/htzhu/mingcheng_new/.codex`;
- create the normal Host backup generated by that install;
- write/push task-owned final closure evidence;
- after all closure conditions pass, delete only the two remote branches named `reviewed/056_product_delivery_discipline`.

The same frozen effects must not be requested again later merely because execution reaches them.

### 3. Kickoff-time drift preflight

Fetch both repos.

Planning baseline before this package:
- AI main `7a6d247e04b05d4371296a9415621b468ed492ce`
- Bridge main `9d2da9f485f26ca51842a1909a276cb44f73351a`

AI planning/review docs will advance main after this baseline; docs-only advance is expected.

Recompute main/reviewed drift before mutation.

Continue only if:
- AI still has no 056 production/version/release overlap beyond the already audited workflow-core TODO plus this planning/review material;
- Bridge main remains ancestor/tree-compatible with exact candidate.

New production/version conflict => STOP Planner/Critic.

### 4. AI integration

From exact latest main in the exact integration worktree, merge the reviewed branch with a real merge commit.

No rebase, squash, cherry-pick, force or history rewrite.

The only allowed conflict is:
`docs/plugin-todos/workflow-core.md`

Resolve it by preserving all newer main-only TODO entries and the reviewed 056 promoted semantics. Mark the 056 delivery/Human-Gate and least-privilege absorption as released in 5.0.7; keep the explicitly deferred post-056 items deferred. Do not implement them.

Any other conflict => STOP.

Release metadata:
- formalize CHANGELOG 5.0.7 with date 2026-09-22 and real-user G1 closure;
- remove stale “real Host pending” release note;
- align affected plugin changelog release dates if needed;
- preserve README human-facing structure and 5.0.7/0.3/0.2/0.4 values;
- update RESULT/evidence manifest from stale “Host pending” to “all gates pass, final integration/permanent install pending”.

Prove the integrated tree is byte-equivalent to `33c30bbe...` for production/version/generated paths defined in the Plan.

Run only:
```text
python scripts/build_codex_marketplace.py --validate --check --path-report
python scripts/skills.py validate
python scripts/skills.py audit --all
python -m unittest tests.test_codex_marketplace
```

Do not run generator --write. Generated drift => STOP.

Do not run full suite/G1-G8/Terra/paid review when production bytes are unchanged.

Freeze the exact AI release commit but do not push it until the Bridge release commit is also frozen.

### 5. Bridge integration

In the exact Bridge integration worktree:
- fast-forward current main through exact candidate `96a8ea1b...`;
- do not alter production code/templates/tests;
- make only release metadata:
  - README current integrated version 0.8.5, not “candidate”;
  - CHANGELOG 0.8.5 released 2026-09-22.

Verify pyproject/runtime/readme/changelog 0.8.5 and prove production source outside README/CHANGELOG is byte-identical to candidate.

No full 372-test rerun and no repeated live G1 smoke.

Freeze exact Bridge release commit.

### 6. Push main

Immediately before push, fetch/recheck both remote mains.

If either has new relevant drift, STOP; do not force.

Push order:
1. Bridge release commit -> Bridge main, non-force.
2. Verify exact remote Bridge SHA.
3. AI release commit -> AI main, non-force.
4. Verify exact remote AI SHA.

If Bridge push fails, do not push AI.
If AI push races/fails after Bridge succeeds, leave valid Bridge main untouched and return Planner/Critic; do not rewrite.

No Git tag, GitHub Release, package-registry publish or deploy.

### 7. Upgrade normal Bridge installation

Only after Bridge main is verified 0.8.5.

Canonical root:
`/overflow/htzhu/mingcheng_new/GPT_Codex_AI_Bridge_Kit`

Preflight ownership/dirty state. Do not stash/reset/restore user work.

If clean:
- fetch origin main;
- pull ff-only to exact released Bridge main;
- inspect current ai-bridge executable/shebang/Python environment;
- refresh the existing editable install from this canonical root using the same environment if needed;
- verify `ai-bridge where` is this canonical root and normal runtime/package identity is 0.8.5.

Do not install normal ai-bridge from the temporary reviewed/integration worktree.

Unrelated dirty user work blocking this exact normal upgrade => STOP rather than choose another source.

### 8. Permanent released Host install

I authorize permanent Host mutation only for:

`/overflow/htzhu/mingcheng_new/.codex`

using the now-normal integrated Bridge 0.8.5.

Run normal:
- `ai-bridge host install --codex-home /overflow/htzhu/mingcheng_new/.codex`
- `ai-bridge host validate --codex-home /overflow/htzhu/mingcheng_new/.codex`

Allow its timestamped backup.

On PASS, leave the released 0.8.5 Host state installed permanently. Do not restore the old 0.8.2-era state.

Do not repeat Human Gate token smoke, Plan smoke or agent-resolvable smoke when production bytes are candidate-equivalent; those final live gates already passed.

If install/validate fails, preserve evidence/backup and perform only a safe bounded restore to the immediate pre-install state if no concurrent drift exists. Do not patch source or broaden policy.

### 9. Final evidence and completion

After both mains + normal Bridge 0.8.5 + permanent Host validate PASS:

write under AI main:
- `results/056_product_delivery_discipline/FINAL_INTEGRATION_RELEASE_CLOSURE.md`
- JSON companion;
- update RESULT/evidence manifest to final truth.

Record exact release SHAs, versions, candidate equivalence, normal Bridge identity, Host backup/validate, prior G1-G8/Source Discovery evidence and README check.

Commit/push this evidence-only closure to AI main non-force.

Only then may `056 overall achieved` be reported.

### 10. Exact branch cleanup

After final evidence main push, verify:
- AI reviewed HEAD is ancestor of AI main;
- Bridge reviewed HEAD is ancestor of Bridge main.

Then delete only the two remote branches:
- AI `reviewed/056_product_delivery_discipline`
- Bridge `reviewed/056_product_delivery_discipline`

No other branch deletion. Do not automatically remove local historical worktrees.

### 11. Still forbidden

- Git tags;
- GitHub Releases;
- package-registry publish;
- deploy;
- paid API/Terra;
- product-repo writes;
- force push / remote remap / history rewrite;
- new Gate/successor;
- Persistent Run refinement;
- unrelated CODEX_HOME/provider/account/credential changes;
- production repair hidden inside integration.

At the end report exact:
- AI release main SHA;
- Bridge release main SHA;
- AI final closure evidence SHA;
- versions;
- production candidate equivalence;
- focused parity results;
- README checked;
- normal ai-bridge root/version;
- permanent Host backup + validate;
- reviewed branch deletion result;
- `SOURCE_DEFECT_DISCOVERED`;
- `056_OVERALL_ACHIEVED=YES|NO`.

`NEXT_HANDOFF=CRITIC`
